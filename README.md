README FOR RIOT API GRAFANA PORT

Script "setup_postgres" sets up database
Script "riot_api_wrapper" messages the riot API using the API key in a .env file inside of the same folder
Script "riot_to_db_postgres" uses the wrapper script to create a table and send that data to the existing postgres database
